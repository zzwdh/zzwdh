---
type Props = {
  title: string;
  copy: string;
};

const { title, copy } = Astro.props;
---

<section id="about" class="section about-section about-section-minimal">
  <div class="section-kicker">ABOUT</div>
  <div class="about-copy">
    <h2>{title}</h2>
    <p>{copy}</p>
  </div>
</section>
